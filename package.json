{
  "name": "openhub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "web",
    "hub",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @openhub/hub",
    "build": "npm run build -w @openhub/web",
    "preview": "npm run preview -w @openhub/web",
    "clean": "rm -rf dist",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  }
}
